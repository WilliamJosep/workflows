name: CI

on:
  push:
    branches:
      - principal
  pull_request:
    branches:
      - principal

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Echo
        run: echo "CI funcionando"
